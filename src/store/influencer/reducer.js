import * as types from "./actionTypes";

const initialState = {
  isFetching: false,
  user: null,
  error: null
};

const influencer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default influencer;
